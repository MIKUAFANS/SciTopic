# Copy this file to `.env` and fill in your own values.
# `.env` is git-ignored and must never be committed.

# LLM API credentials (used by the online LLM query client).
SCITOPIC_LLM_API_KEY=your-api-key-here
SCITOPIC_LLM_BASE_URL=https://api.openai.com/v1
